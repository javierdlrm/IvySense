import { Routes } from '@angular/router';
import { TermsOverviewComponent } from '@features/terms/overview/terms-overview.component';

export const routes: Routes = [
  { path: 'dashboard', component: TermsOverviewComponent }
];
